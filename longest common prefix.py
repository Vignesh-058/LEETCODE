def lcp_h(strs):
  if len(strs) == 0:
    return ""
  prefix = strs[0]
  for i in range(1,len(str)):
    while str[i].find(prefix) != 0:
      prefix == prefix [0:len(prefix)-1]
      if prefix  == "" :
        return ""
  return prefix

def lcp_v(strs):
  if strs == NONE or len(strs) == 0:
    return ""
  for i in range(i,len(strs[0]):
    c = str[0][i]
    for j in range(i+1,len(strs)):
      
    
  
